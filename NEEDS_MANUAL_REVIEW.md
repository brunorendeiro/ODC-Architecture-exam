# Needs Manual Review

Audit date: 2026-07-22

These records were reviewed but not promoted to verified status because a directly supporting current official source was not established, the claim is infrastructure-specific, or the source record is incomplete. Existing answers are retained for traceability and must not be treated as authoritative.

## Runtime questions (12)

| ID | Question | Reason | Candidate official URL(s) |
|---:|---|---|---|
| 92 | What is the purpose of the Guidewire connector in the Billing app? | NOT CONFIRMED: the answer is architecturally plausible, but this audit did not establish a sufficiently direct official statement for the exact wording. Use the linked official material to research the scenario before relying on it. | https://success.outsystems.com/documentation/outsystems_developer_cloud/app_architecture/integration_patterns/<br>https://www.outsystems.com/training/classroom-training/odc-architecture-boot-camp/ |
| 95 | What should be done after mapping bounded contexts to apps and libraries? | NOT CONFIRMED: the answer is architecturally plausible, but this audit did not establish a sufficiently direct official statement for the exact wording. Use the linked official material to research the scenario before relying on it. | https://success.outsystems.com/documentation/outsystems_developer_cloud/app_architecture/architecture_design_process/<br>https://www.outsystems.com/training/classroom-training/odc-architecture-boot-camp/ |
| 97 | What is a key feature of the SAP connector in ODC? | NOT CONFIRMED: the answer is architecturally plausible, but this audit did not establish a sufficiently direct official statement for the exact wording. Use the linked official material to research the scenario before relying on it. | https://success.outsystems.com/documentation/outsystems_developer_cloud/app_architecture/integration_patterns/<br>https://www.outsystems.com/training/classroom-training/odc-architecture-boot-camp/ |
| 112 | What can reusable components and external integrations provide for bounded contexts? | NOT CONFIRMED: the answer is architecturally plausible, but this audit did not establish a sufficiently direct official statement for the exact wording. Use the linked official material to research the scenario before relying on it. | https://success.outsystems.com/documentation/outsystems_developer_cloud/app_architecture/architecture_design_process/<br>https://www.outsystems.com/training/classroom-training/odc-architecture-boot-camp/ |
| 70 | What happens to metadata when a connection to an external system is deleted in ODC? | NOT CONFIRMED: the answer is architecturally plausible, but this audit did not establish a sufficiently direct official statement for the exact wording. Use the linked official material to research the scenario before relying on it. | https://success.outsystems.com/documentation/outsystems_developer_cloud/integration_with_external_systems/integrate_with_external_data_sources_using_data_fabric/ |
| 130 | What is the purpose of the version number in ODC libraries? | NOT CONFIRMED: the answer is architecturally plausible, but this audit did not establish a sufficiently direct official statement for the exact wording. Use the linked official material to research the scenario before relying on it. | https://success.outsystems.com/documentation/outsystems_developer_cloud/building_apps/libraries/<br>https://learn.outsystems.com/training/journeys/odc-architecture-exam-957/libraries/odc/4374<br>https://www.outsystems.com/training/classroom-training/odc-architecture-boot-camp/ |
| 134 | How are strongly coupled domains handled in ODC architecture? | NOT CONFIRMED: the answer is architecturally plausible, but this audit did not establish a sufficiently direct official statement for the exact wording. Use the linked official material to research the scenario before relying on it. | https://success.outsystems.com/documentation/outsystems_developer_cloud/app_architecture/architecture_design_process/<br>https://www.outsystems.com/training/classroom-training/odc-architecture-boot-camp/ |
| 139 | Which statement aligns with best practices for managing app ownership during the architecture design process? | NOT CONFIRMED: the answer is architecturally plausible, but this audit did not establish a sufficiently direct official statement for the exact wording. Use the linked official material to research the scenario before relying on it. | https://success.outsystems.com/documentation/outsystems_developer_cloud/app_architecture/architecture_design_process/<br>https://www.outsystems.com/training/classroom-training/odc-architecture-boot-camp/ |
| 184 | An architect proposes creating 47 separate ODC Apps — one for each business function in the company. A colleague argues this is too granular. What is the main risk of extreme over-fragmentation? | NOT CONFIRMED: the answer is architecturally plausible, but this audit did not establish a sufficiently direct official statement for the exact wording. Use the linked official material to research the scenario before relying on it. | https://success.outsystems.com/documentation/outsystems_developer_cloud/app_architecture/architecture_design_process/<br>https://www.outsystems.com/training/classroom-training/odc-architecture-boot-camp/ |
| 189 | A developer discovers that a date-sorting utility is used in 3 different Apps and immediately proposes extracting it into a Library. A senior architect cautions against premature extraction. What is the main risk of always centralising every reusable piece of logic into a Library? | NOT CONFIRMED: the answer is architecturally plausible, but this audit did not establish a sufficiently direct official statement for the exact wording. Use the linked official material to research the scenario before relying on it. | https://success.outsystems.com/documentation/outsystems_developer_cloud/building_apps/libraries/<br>https://learn.outsystems.com/training/journeys/odc-architecture-exam-957/libraries/odc/4374<br>https://www.outsystems.com/training/classroom-training/odc-architecture-boot-camp/ |
| 191 | An architect consolidates 12 ODC components into just 3, reasoning: 'Fewer components means simpler architecture.' A reviewer flags this as a potential problem. What is the main risk of over-consolidation? | NOT CONFIRMED: the answer is architecturally plausible, but this audit did not establish a sufficiently direct official statement for the exact wording. Use the linked official material to research the scenario before relying on it. | https://success.outsystems.com/documentation/outsystems_developer_cloud/app_architecture/architecture_design_process/<br>https://www.outsystems.com/training/classroom-training/odc-architecture-boot-camp/ |
| 204 | A producer App publishes an 'InvoiceGenerated' event. Six months later, the team needs to add a new field to the event payload. Existing consumers depend on the current payload structure. What is the correct approach? | NOT CONFIRMED: the answer is architecturally plausible, but this audit did not establish a sufficiently direct official statement for the exact wording. Use the linked official material to research the scenario before relying on it. | https://success.outsystems.com/documentation/outsystems_developer_cloud/building_apps/about_event_driven_architecture/events_reference/<br>https://www.outsystems.com/training/classroom-training/odc-architecture-boot-camp/ |

## OCR/import records (65)

All 65 records in `files/odc_udemy_questions_for_app_import (1).json` require reconstruction. Several have merged question/option text, missing choices, or no correct answer. They are not loaded by the simulator.

- IMPORT-1: Which of the following options is NOT an element considered when identifying the app’s concepts? User stories O Business requirements O — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-2: Based on the best practices, which of the following scenarios would be considered ideal for application ownership and sponsorship in ODC? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-3: How are libraries incorporated into applications in ODC? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-4: A single team can own multiple applications in ODC. What is the crucial factor to consider when assigning application ownership to a team? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-5: The image below shows the Order Management bounded context and the application concepts (Customer, Order, Product, and Salesforce), defined during the Architecture Design Process. Considering the naming best practices, which of the following options is correct? gt Order Management --+-++-+-+-+04, The app’s elements, such as Actions, Screens, or Entities, should be prefixed with the application concept name, followed by a clear name stating its purpose. For rv) instance, an Action to update the product stock could be called ProductUpdateStock. The applications should be named after the bounded context they represent, while the Libraries’ naming should consider the context where they are being used, — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-6: What is the key principle for managing dependencies between apps in ODC? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-7: When an ODC application is published, what happens to the libraries it depends on? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-8: Considering the Architecture Design Process, which of the following options is correct? The main goal of the Assemble step is to identify the bounded context while (e) ensuring that dependencies between these contexts are as few as possible. During the Organize step, the previously identified bounded contexts are mapped into ODC apps while promoting independence between the development teams and — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-9: Starting with a simple application architecture and increasing complexity as needed. What is the main benefit of this approach? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-10: What is the purpose of the Callback endpoint in a near real-time Integration Pattern? To support the creation of audit logs specific for the integration, to facilitate the — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-11: When working with a single development team and a simple application architecture, what advantage is gained by keeping all functionalities within one application? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-12: What is a key benefit of ensuring independent release cycles for development teams in ODC? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-13: shared with the Orders and Shipping apps. The consumer apps use Aggregates to fetch the data and perform calculations with it based on complex business rules. According to the OutSystems best practices, what could be improved in this salution, if anything? = a Sere on one rk — oe [ J I oe \| a \| — — This solution is the best approach since it is the only one that helps consumer apps and their teams manage best any change or evolution of the business rules and — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-14: In ODC, what role is typically responsible for prioritizing application change requests based on budget? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-15: What is the primary benefit of containerizing apps in ODC deployments? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-16: In ODC, how can an app access data from another app? (Choose TWO) — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-17: In ODC, a single app can be used for all of the following EXCEPT: — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-18: Considering the ownership of apps, which of the following options describes a best practice that should be followed during the Architecture Design process? A business owner should own a single app, as owning multiple apps would lead to — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-19: How does containerization contribute to the scalability of ODC applications? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-20: In the context of a single application managing all business concepts, how does feature toggling help manage independent delivery of features? jows for selectively enabling or di: ing res for users or ry It all for selectir abli disabling features for different deployment stages. — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-21: Considering the reusability between ODC apps and Libraries, which of the following options is correct? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-22: It's reccomended to split a complex application into separate applications primarily to achieve: — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-23: What is the main drawback of having two business owners for a single application? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-24: What is the MAIN benefit of independent app scaling in a cloud-native architecture with ODC? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-25: When is it acceptable to have two business sponsors involved with an ODC application deployment? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-26: Consider the following scenario where a client uses the Home Insurance app to subscribe to an Insurance Policy. Knowing that step 4 can sometimes fail due to network connectivity issues, what is the best a oye we rae at ASL, Teens mre ttt Chee —_ o 2 > > 2 2 9g. ee a oe — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-27: How does independent life-cycle independence among teams benefit development in ODC? \| Your answers correct — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-28: Consider you have the Role of HR Manager in the Directory app. The HR Manager Role will also be important in the Vacations app since some functionalities are only accessible or available to users with that Role. How should we define the Roles and assign them to the respective users to make sure we can implement the desired functionalities on both apps? \| Oectory, Yneatiora iManage Set the Role as Public in the Directory app and reference the Role in the Vacations — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-29: Which of the following best describes the core concept behind a cloud-native application architecture in ODC? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-30: Consider two applications that will share the same Theme, resulting in the following blueprint. Besides the Theme, these apps will also have UI patterns implemented using Blocks that will also be shared between these apps, and other UI patterns that will be added in the future. What is the best approach to implementing the UI patterns? oat i=] \| — { Weosdons Theme = Theme Create one application to implement the UI patterns. This new application and its patterns can then reference the Theme. ° The consumer apps would then reference the Blocks that implement the UI patterns directly. \| Your answeris correct Create one Library to implement the Patterns. ° Then, both consumer applications can reference the elements directly from the Theme and UI patterns Libraries. Create one application to implement the Patterns. — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-31: What is the role of libraries in OutSystems Developer Cloud? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-32: What happens when an app is published in OutSystems Developer Cloud? ps \| — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-33: What is the first step in converting an O11 architecture blueprint to an ODC architecture blueprint? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-34: In the process of mapping O11 app architecture to ODC, what should be done if multiple bounded contexts are identified in a domain? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-35: What indicates a need for further refining an app's architecture during the conversion process? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-36: Which of the following is NOT a basis for grouping business concepts into bounded contexts? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-37: What is suggested by the presence of multiple bounded contexts within a single domain? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-38: In what scenario might you choose to split a domain into smaller domains? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-39: In OutSystems Developer Cloud, how are dependencies between apps maintained? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-40: When mapping O11 architecture to ODC, what is a critical aspect to analyze for each bounded context? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-41: In ODC, what is the key difference between Libraries and traditional applications (Web/Mobile)? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-42: Consider the following statement: "Deploying an app to Production in ODC does not affect the app running in Development.” What concept does this describe? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-43: Effective code reuse in ODC can help achieve: O._ Difficulty in managing application versions — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-44: How can you achieve the functionality of a public Server Action in ODC? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-45: When following best practices for code reusability in ODC, it's recommended to: \| Your answers correct — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-46: When sharing elements between apps in ODC, what kind of dependency is created? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-47: Imagine a warning appears during the impact analysis stating that an app references a missing producer application. — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-48: Which of the following is NOT a typical use case for a service action in ODC? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-49: What kind of dependency is created when Web Apps or Mobile Apps share elements like Service Actions, Entities, Static Entities, and Screens? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-50: Consider the following statement: "Client Actions and Server Actions can be shared from Libraries to Web or Mobile Apps." What type of dependency describes this scenario? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-51: What type of elements can be included within libraries? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-52: Compared to strong dependencies, weak dependencies between apps in ODC offer: — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-53: Imagine you're building an application that displays weather information. How could a service action be helpful in this scenario? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-54: Imagine a scenario where you need to update a branding style across your applications in ODC. Which element type is most suitable for this change, considering reusability? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-55: In ODC, what is the main benefit of sharing public elements between applications? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-56: When deploying an app to Production in ODC, what does the "Impact analysis" feature do? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-57: Imagine an app reuses a static entity from another app. This scenario represents a: — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-58: In ODC, which type of app can act as both producer and consumer when sharing elements? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-59: Which of the following statements is true about sharing Client Actions and Server Actions in ODC? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-60: Consider the following statement: "The ODC Portal displays the stage on which an app is deployed.” What does "stage" refer to in this context? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-61: Why can't you directly set a Server Action to Public within an app in ODC? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-62: What is the primary purpose of setting an element's Public property to Yes in ODC? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-63: When should you consider splitting a bounded context implementation into separate applications? ©. Toreduce the overall development effort for the application ©. Toimprove the scalability of the application for future growth ©. Tosimplify the process of managing user authentication — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-64: What should be done if different bounded contexts have different business owners? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
- IMPORT-65: What is the main benefit of mapping bounded contexts to ODC apps and libraries? — OCR/import source is not used by the runtime simulator and contains incomplete or corrupted question text/options. Manual reconstruction against an official source is required before import.
